<div class="max-w-2xl mx-auto">
  <%= link_to [@entry.project, @entry], class: "text-sm text-[var(--color-primary)] hover:text-[var(--color-secondary)] transition-colors inline-flex items-center gap-1 mb-6" do %>
    ← Back to Entry
  <% end %>

  <h1 class="text-2xl font-bold mb-6">Edit Journal Entry</h1>

  <%= render "form", project: @project, entry: @entry %>
</div>
