<div class="max-w-2xl mx-auto">
  <%= link_to @project, class: "text-sm text-[var(--color-primary)] hover:text-[var(--color-secondary)] transition-colors inline-flex items-center gap-1 mb-6" do %>
    ← Back to <%= @project.name %>
  <% end %>

  <h1 class="text-2xl font-bold mb-6">Connect AI Harness</h1>
  <p class="text-[var(--color-text-muted)] text-sm mb-6">Link your AI agents to this project for seamless integration.</p>
  <%= render "form" %>
</div>
